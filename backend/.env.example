# Server Environment Configuration for Vercel & Local
PORT=5000
NODE_ENV=production
JWT_SECRET=super_secret_sales_analytics_key_2026_mca_project

# Database Config: 'sqlite' or 'mysql'
DB_TYPE=sqlite
# For Vercel Serverless, ':memory:' is used automatically when VERCEL=1
DB_FILE=:memory:

# MySQL Configuration (Optional if using cloud MySQL e.g. PlanetScale, Aiven, RDS)
# MYSQL_HOST=your-db-host.com
# MYSQL_USER=your_db_user
# MYSQL_PASSWORD=your_db_password
# MYSQL_DATABASE=sales_analytics_db
